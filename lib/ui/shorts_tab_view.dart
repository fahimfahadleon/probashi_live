import 'package:flutter/material.dart';

class ShortsTabView extends StatelessWidget {
  const ShortsTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Shorts", style: TextStyle(color: Colors.white)),
    );
  }
}